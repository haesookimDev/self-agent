{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the Continuum application window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
